---
"@humanlayer/effect-machine": patch
---

Preserve exact machine schema types on `MachineEntity` and require `.task()` shorthand to return a valid machine event without using `any` or diagnostic suppressions.
